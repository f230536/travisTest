language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
before_script:
  - npm install -g phantomjs
  - npm install -g chromedriver

script: 
- cd main && lettuce -v 3 features/simple.feature chrome desktop-m
